# Pipelined-Floating-Point-Adder
A 4-stage pipelined IEEE-754 single-precision floating-point adder designed in Verilog, with a Python-based verification environment that generates random FP32 test vectors, runs RTL simulation, compares hardware outputs against expected results and reports passed/failed statistics along with the maximum and average error across tests. 
